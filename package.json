{
  "name": "bookverse",
  "private": true,
  "description": "Loja BookVerse — site estático + funções serverless (Firebase Hosting + Functions)",
  "scripts": {
    "deploy": "firebase deploy",
    "emulador": "firebase emulators:start"
  },
  "dependencies": {
    "jpeg-js": "^0.4.4"
  }
}
